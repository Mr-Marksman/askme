<div class="container mb
  <%= "hidden" if question.hidden? %>
  <%= "user_question" if current_user == question.user && !current_user.nil? %>">

  <p class="mb-sm flexbox-between"> 
    <div class="question_link">
      <%= link_to "Question #{question.id}", question_path(question), class: "link", tabindex: "1" %>:
    </div>
    
    <div class="question_text">
      <p>
        <% if question.topic.present? %>
          <%= text_with_hashtags(question.topic)&.truncate(80, separator: " ") %>
        <% else %>
          <p>"Question without topic (old version):</p>
          <p>
            <% splited_text(question.body.truncate(60, separator: " ")).each do |line| %>
              <%= line %><br>
            <% end %>
          </p>
        <% end %>
      </p>
    </div>
  </p>
</div>
